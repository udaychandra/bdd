project.ext {
    pomName = "UD BDD Core library"
    pomDescription = "Core library used by the JUnit 5 based UD BDD library"
}

if ("$pubToMC" == "yes") {
    apply from: "${project.rootDir}/publish.gradle"
}
